<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lamp.dao.TSystemOperationMapper" >
  <insert id="saveLogData" parameterType="java.util.HashMap">
    insert into oper_log (id,userid,menu_id,kind_id,operdes,opertime) values
    (#{id},#{userId},#{menu_id},#{kind_id},#{operDes},#{operTime})
  </insert>
</mapper>